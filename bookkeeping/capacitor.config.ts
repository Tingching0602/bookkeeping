import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.pear.bookkeeping',
  appName: '梨子小帳本',
  webDir: 'dist',
  bundledWebRuntime: false,
}

export default config
